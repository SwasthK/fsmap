// import { describe, test, expect } from "vitest";
// import path from "path";
// import { buildFsTree } from "../src/utils/helper/reader";

// describe("buildFsTree", () => {
//   test("reads file tree of sample folder", async () => {
//     const target = path.resolve(__dirname, "sample");

//     const tree = await buildFsTree(target);
//     expect(tree.name).toBe("sample");
//     expect(tree.isDirectory).toBe(true);
//     expect(tree.children?.length).toBeGreaterThan(0);
//   });
// });
